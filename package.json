{
  "name": "esbuild-aem-microfrontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=public/index.js",
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit": "^2.1.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.18",
    "http-server": "^14.1.0"
  }
}
